polinomio :: Int -> Int
polinomio x = x*x + 10*x +2

-- Cálculo do quadrado de um número inteiro
quadrado :: Int -> Int
quadrado x = x*x

-- soma de dois números
soma :: Float -> Float -> Float
soma a b = a + b

triplica :: Int -> Int
triplica x = x*3